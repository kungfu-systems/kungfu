# OS files
.DS_Store

# IDE related
.idea/
.vs/
.vscode/
.arcconfig
.project
*.VC.*
*.sln
*.vcxproj*
*.flow
*.log
!docs/qualification/evidence/durability/**/*.raw/*.log

# Node related
node_modules/

# Python related
__pycache__/
__pypackages__/
.pdm.toml
*.pyc
*.egg-info/
*.egg

# Kungfu related
__kungfulibs__/
.kungfu/runtime/

# Xinfa related
# Baseline material (atlas.json, views, packs) is a local immutable store and
# stays out of Git; only the witness manifest and receipt of every baseline
# layer are tracked (ADR-0097 §7).
.xinfa/baselines/sha256/*/**
!.xinfa/baselines/sha256/*/**/
!.xinfa/baselines/sha256/*/**/manifest.json
!.xinfa/baselines/sha256/*/**/receipt.json

# Build related
build/
build-*/
target/
cmake-build-debug/
dist/
.buildchain/*
!.buildchain/buildchain.toml
!.buildchain/alpha-contract-lock.json
!.buildchain/contract-lock.json
product/.buildchain/
!.buildchain/kfd/
.buildchain/kfd/*
!.buildchain/kfd/kfd-1/
!.buildchain/kfd/kfd-1/**
!.buildchain/kfd/kfd-2/
!.buildchain/kfd/kfd-2/**
!.buildchain/kfd/kfd-3/
!.buildchain/kfd/kfd-3/**

# Debug related
debug-example/
.cache/
.lycheecache

# assemble demo result
*.csv
*.xls
*.xlsx

# flatc build-time generated files (produced by framework/core/CMakeLists longfist_fb_codegen
# during the build, not committed; the hand-written sources *.fbs / *_fb_builder.h stay tracked)
framework/core/src/libkungfu/include/kungfu/longfist/fb/*_generated.h
framework/core/src/libkungfu/include/kungfu/longfist/fb/*.bfbs

# Local cache/mirror proxy per-checkout override (optional); the real values and primary config
# live in the user-global ~/.config/kungfu/build-local.env, not committed
/build-local.env
demo-runtime/
