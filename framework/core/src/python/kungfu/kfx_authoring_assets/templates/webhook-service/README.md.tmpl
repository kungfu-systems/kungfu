# {{PACKAGE_TITLE}}

This ordinary service KFX was generated by Kungfu {{PRODUCT_VERSION}} from
authoring kit `{{KIT_ROOT}}` and runtime SDK `{{SDK_ROOT}}`.

It owns only webhook normalization and the post-authentication domain callback.
Core owns KFD assessment, Warrant and Passport admission, capability grants,
host authorization, lifecycle state, and receipts. The credential broker owns
credential material; this package receives only the opaque handle
`credential:webhook/signing`.

Run the installed-only qualification with:

```sh
kungfu kfx author validate . --json
kungfu kfx author qualify . --json
```

The fixture binds loopback on an ephemeral port. Replace its synthetic event
callback with domain behavior, but do not move provider authentication into
Core or retain request bodies, headers, signatures, broker errors, or secrets.

