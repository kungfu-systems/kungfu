# GUI capability-boundary evidence at `611e39d82`

The adjacent report records the clean macOS arm64 qualification and installed
promotion of Product artifact `20260714T154905Z-611e39d82`. The candidate
contains canonical packaging fix `3e9d6e20708233d9316ad2e02b5d2d5b7ab50f90`.

The source gate passed 268 cases. The Product distribution and explicit bundle
audit resolved seven external main dependencies. The packaged app reached
`KF_GUI_QUALIFICATION_READY`, then completed all six authenticated Codex
structured-transport lifecycle cases with Codex `0.144.3`. Promotion advanced
the installed receipt to the exact build, an Electron renderer appeared, the
Agent Session worker and native helper were present, and all three existing
detached tmux sessions remained listed.

## Claim boundary

This is macOS arm64 machine evidence. Linux, Windows, physical machine restart,
and interactive pixel inspection were not run. The provider qualification
retained neither raw terminal content nor private environment values. Two
pre-promotion application copies were deliberately kept outside the repository
as local rollback material and are not release artifacts.
