---
metadata_schema: kungfu.document-metadata/v1
doc_type: architecture-decision
adr_id: KF-ADR-019f9f04-f8bd-7002-a702-1b9f66827ec0
decision_status: accepted
implementation_status: implemented
implementation_prs: [https://github.com/kungfu-systems/kungfu/pull/1551, https://github.com/kungfu-systems/kungfu/pull/1553, https://github.com/kungfu-systems/kungfu/pull/1563, https://github.com/kungfu-systems/kungfu/pull/1578, https://github.com/kungfu-systems/kungfu/pull/2000, https://github.com/kungfu-systems/kungfu/pull/2012, https://github.com/kungfu-systems/kungfu/pull/2284, https://github.com/kungfu-systems/kungfu/pull/3346, https://github.com/kungfu-systems/kungfu/pull/3358, https://github.com/kungfu-systems/kungfu/pull/3374, https://github.com/kungfu-systems/kungfu/pull/3421]
closure_pr: https://github.com/kungfu-systems/kungfu/pull/1578
qualification_refs: [framework/maintainability/code-complexity-policy.json, framework/maintainability/code-complexity-baseline.json, framework/maintainability/complexity-governance.mjs, framework/maintainability/baseline-transitions/README.md, scripts/code-complexity-budget.mjs, scripts/code-complexity-budget.test.mjs, framework/maintainability/source-analysis-kernel.mjs, framework/maintainability/source-analysis-session.mjs, framework/maintainability/function-risk-transition.mjs, framework/maintainability/function-risk-ratchet-policy.json, framework/maintainability/function-risk-ratchet.mjs, framework/maintainability/function-risk.mjs, framework/maintainability/function-risk.test.mjs, framework/maintainability/fixtures/function-risk-legacy-shadow.mjs, extensions/work-control/work-control-actions/adapter.py, extensions/work-control/work-control-actions/domain/work_control_assessment.py, framework/core/src/python/kungfu/release_provenance/__init__.py, framework/core/src/python/kungfu/release_provenance/_v1_graph.py, framework/maintainability/semantic-amplification.manifest.json, framework/maintainability/semantic-amplification-report.json, framework/maintainability/semantic-amplification.fixtures.json, framework/maintainability/semantic-amplification.mjs, framework/maintainability/semantic-amplification.test.mjs, framework/gui/electron-builder.base.json, framework/gui/scripts/electron-builder-config.test.mjs, product/electron-builder.overlay.json, product/scripts/dist.test.mjs, product/scripts/runtime-pin-snapshot.mjs, framework/core/src/libyijinjing/src/io/durability.h, framework/core/src/libyijinjing/src/io/durability.cpp, framework/core/src/libyijinjing/tests/durability_tests.cpp, framework/maintainability/terminal-evidence-matrix.json, framework/maintainability/terminal-evidence-matrix.test.mjs, framework/maintainability/readonly-source-routes.json, scripts/check-readonly-source-routes.mjs, scripts/check-readonly-source-routes.test.mjs, scripts/shifu-readonly-entry.mjs, scripts/readonly-agent-bootstrap.test.mjs, scripts/check-shifu-entry-contract.test.mjs, scripts/source-acceptance.mjs, scripts/source-acceptance.test.mjs, .github/workflows/affected-native-pr.yml, .github/workflows/alpha-promotion-preflight.yml, .github/workflows/build.yml]
review_state: self-reviewed
sensitivity: public
sources: [architecture-decisions, local-files, user-consensus]
period: 2026-07-26
theme: maintainability-governance
confidence: high
evidence_grade: B
last_reviewed: 2026-08-25
ai_provenance: GPT-5 via Codex on 2026-07-31, 2026-08-22, 2026-08-23, and 2026-08-25; based on repository contracts, adversarial topology and changed-function fixtures, deterministic packaging projections, executable legacy/successor shadow parity, local native compilation, physically locked source qualification, exact-head workflow selectors, and user-authorized maintainability goals; installed-product, cross-platform artifact, physical power-loss, and public release qualification are not claimed
---

# KF-ADR-019f9f04-f8bd-7002-a702-1b9f66827ec0: Maintainability governance uses read-only composite regression ratchets

- Status: accepted and implemented; terminal evidence remains exact-head bound
- Date: 2026-07-26

## Context

Kungfu has more than 440,000 source lines across more than 5,400 tracked
source files. A semantic change may cross generated projections, language
boundaries, release gates, and product surfaces even when the edited file is
small. File length alone therefore cannot identify the responsibility,
dependency, fan-out, churn, amplification, or ownership costs that make a
change hard to reason about.

The architecture health budget was already close to its admitted threshold,
and the protected checkout deliberately rejects writes. Discovery that
installs dependencies, creates caches, generates files, or mutates the
checkout cannot be the source-level entry point for either an agent or a
maintainer. A maintainability floor must remain useful before a build exists
and must not convert historical debt into a blanket exemption for new debt.

## Decision

Kungfu adopts a read-only composite regression ratchet as the source-level
maintainability floor.

### Zero-write discovery

Build-free discovery and source acceptance must not install dependencies,
create caches, contact the network, generate files, or otherwise mutate the
checkout. The read-only Shifu entry resolves only committed repository
authorities and delegates mutating or build-dependent work to explicit later
stages.

### Composite budgets

Maintainability budgets classify files by language and responsibility instead
of applying one global line limit. The first stage enforces:

1. a hard cap for newly introduced eligible files;
2. non-growth for pre-existing files already above their classified budget;
3. exact baselines for admitted historical debt; and
4. deterministic reports suitable for source gates and review evidence.

Length is never accepted as a complete fitness signal. Subsequent
decomposition decisions must combine responsibility, dependencies, fan-out,
change history, semantic amplification, and ownership.

### Independent, expiring exceptions

An exception must identify an exact path, owner, independent approver,
rationale, expiry, and removal condition. The author cannot approve their own
exception. Exceptions never mutate the permanent baseline and cannot match
future files by pattern.

### Authority boundary

The semantic amplification graph is a projection over existing repository
authorities. It may expose which surfaces and owners a change reaches, but it
does not become a second semantic authority or redefine those surfaces.

The abstraction-integrity extension keeps that boundary by naming one owner,
one existing authority, legal variation axes, adapters, qualification routes,
and bounded exceptions for each representative topology. Its build-free
findings and weighted-debt ratchet classify parallel authority, duplicated
policy, leaked platform decisions, orphan adapters, and stale exceptions
without flattening legitimate provider, lifecycle, product, or durability
semantics. Product assembly has one explicit lifecycle-order owner. Desktop
packaging now materializes executable framework and product projections from
one base plus a product-only overlay, and fails source acceptance if either
projection drifts. Native file and directory durability mechanics now pass
through one internal cross-platform adapter while strict, best-effort, and
provider-owned durability semantics remain separate at their domain call
sites. These remediations reduce the representative abstraction-integrity
ratchet from three findings and weighted debt 18 to zero without widening an
exception.

### Terminal qualification boundary

Pull request 1551 proves the read-only source-governance stage. Authority
convergence, responsibility-hotspot decomposition, Linux product
qualification, and Windows product qualification remain separately owned
follow-up Assignments. This decision does not claim physical durability,
public release, or qualification on an unobserved platform.

The terminal hardening candidate adds cryptographically bound independent
waiver and baseline-transition governance, rejects responsibility-preserving
split and classification-laundering variants, inventories every supported
read-only route, and runs the complete source gate from a physically locked
cold checkout. It also retires duplicated KFX forwarding decisions and
extracts cohesive complexity-governance and Buildchain KFD runtime seams.
The versioned
[`terminal-evidence-matrix.json`](../../framework/maintainability/terminal-evidence-matrix.json)
binds all seven required Assignments to the exact protected commit being
qualified. It names the source, preflight, product, artifact, native Assignment,
and fresh independent-review selectors that must all resolve before terminal
settlement. A green run from another commit, a private-chat summary, or a
partially settled child cannot satisfy that matrix.

Pull request 2284 extends this implemented decision with the stage-ready
repository-hotspot decomposition: the Work Control, SDK, storage service, and
distribution owners are split into cohesive seams while the exact-head
complexity, semantic-amplification, architecture, source, native-build, and
independent-review evidence remains required. The pull request is implementation
evidence only after protected publication; this record does not substitute its
open-PR checks for the terminal matrix's protected-cut requirement.

Pull request 3346 extends the same decision with a stage-ready convergence of
quality analysis ownership. Complexity, function-risk, and semantic
amplification consumers share one read-only source-analysis kernel for Git
inventory, language classification, digesting, line measurement, and
four-language function extraction. An executable copy of the protected-base
function-risk implementation remains test-only long enough to prove fixed
fixture and exact-repository successor parity; production consumers do not
retain a second authority. Repeated analysis may reuse a disposable cache only
outside the checkout and only when commit, tree, status, baseline, policy,
input-contract, and implementation roots match exactly; malformed or mismatched
entries fail closed. The protected merge and post-merge exact-head source gate
remain required before this PR becomes implementation evidence.

Pull request 3358 extends that shared analysis boundary with a stage-ready
changed-code function-risk ratchet. The required fast phase compares only the
exact candidate paths with the exact protected base: existing functions may not
increase base risk, new functions must remain inside fixed language-family
envelopes, and same-owner wrapper extraction cannot reset aggregate risk.
Unchanged historical debt remains advisory rather than becoming a repository-
wide blocker. Exact path and symbol identity is preferred before bounded
same-owner movement, so unrelated owners with common symbol names do not form a
false transition. A single-process analysis session reuses each exact changed-
file parse between the ratchet and the unchanged full advisory projection;
external cache identity remains bound to source, base, policy, inputs, and
implementation roots. Fixed positive and negative fixtures plus real-repository
shadow parity, protected merge, and post-merge exact-head qualification remain
required before this PR becomes implementation evidence.

Pull request 3374 completes the bounded self-governance cutover for that
analysis boundary. A navigation-only responsibility map names the distinct
authority, required-gate, shared-kernel, shadow, diagnostic, advisory, and test
owners; the semantic-amplification gate checks those ownership claims and
fails closed on blocking topology drift without becoming a replacement
authority. Transition matching reuses the shared changed-function references
and preserves same-owner aggregate risk across exact body movement, while the
quality tools themselves remain inside the function-risk and wrapper-only
ratchets. Fixed fixtures and a real repository snapshot preserve findings,
classification, severity, and exit-status parity. The bounded stage reduces
quality-owner aggregate base risk from 4032 to 2328, maximum function risk from
847 to 138, and self-owned wrapper-only findings from one to zero; the exact
external-cache benchmark reduces combined warm fast-plus-full time by 44.4%.
These measurements are review evidence, not new thresholds. Protected merge
and a post-merge exact-head source gate remain required before this PR becomes
implementation evidence.

Pull request 3421 extends the same stage-ready boundary across the Python Work
Control and release-provenance owners. Work assessment and adapter orchestration
retain their public contracts while large decision paths are decomposed into
cohesive helpers, and the release-provenance v1 graph moves behind a private
module instead of remaining embedded in the public facade. The bounded cut
reduces the targeted maximum function risk from 2063 to 148 without introducing
a second policy or provenance authority. Exact-head function-risk,
abstraction-integrity, source, Profile SDK, and KFX impact qualification,
protected merge, and post-merge source qualification remain required before
this pull request becomes implementation evidence.

## Consequences

- Maintainers and agents receive a useful architecture and complexity signal
  before dependency installation or native compilation.
- New complexity cannot hide inside the historical baseline, while admitted
  legacy hotspots can be reduced incrementally.
- Reviews gain an explicit map from semantic families to existing authorities
  and downstream surfaces.
- Policy, baseline, and graph manifests add maintenance cost. Exact stale-entry
  and expiry checks make that cost visible rather than silently weakening the
  gate.
- A passing source gate is not proof that a large file has coherent
  responsibilities, nor is it product qualification. The retained follow-up
  Assignments own those stronger claims.

## Falsifiers

This decision is violated if read-only discovery writes to the checkout,
contacts the network, or requires installed dependencies; if a new
over-budget file passes without an exact independent exception; if a
pre-existing over-budget file grows without a valid exception; if line count
is treated as the sole fitness signal; or if the amplification projection
becomes a competing semantic authority.
