---
metadata_schema: kungfu.document-metadata/v1
doc_type: architecture-decision
adr_id: KF-ADR-019f86da-4f90-710c-a3b6-5e0cb5a28ad0
decision_status: accepted
implementation_status: staged
implementation_prs: [https://github.com/kungfu-systems/kungfu/pull/1142, https://github.com/kungfu-systems/kungfu/pull/1143, https://github.com/kungfu-systems/kungfu/pull/1145, https://github.com/kungfu-systems/kungfu/pull/2187, https://github.com/kungfu-systems/kungfu/pull/2209, https://github.com/kungfu-systems/kungfu/pull/2233, https://github.com/kungfu-systems/kungfu/pull/2238, https://github.com/kungfu-systems/kungfu/pull/2240, https://github.com/kungfu-systems/kungfu/pull/2294]
qualification_refs: [framework/action/cli-topology.contract.json, framework/core/src/python/kungfu/__main__.py, framework/core/src/python/kungfu/cli/catalog_projection.py, framework/core/src/python/kungfu/cli/commands/dev.py, framework/core/src/python/kungfu/cli/commands/agent_first_value_entry.py, framework/core/src/python/kungfu/cli/help_projection.py, framework/core/src/python/kungfu/cli/surface_contract.registry.json, framework/core/src/python/kungfu/agent/cli_surface.catalog.json, framework/core/src/python/kungfu/agent/first-value.contract.json, framework/core/src/python/kungfu/agent/first-value-receipt.schema.json, framework/agent-session/kungfu-agent-session.contract.json, framework/core/src/python/kungfu/agent/bootstrap-receipt.schema.json, framework/core/tests/python/test_agent_first_entry.py, framework/core/tests/python/test_agent_console_contract.py, framework/core/tests/python/test_cli_progressive_help.py, framework/core/tests/python/test_cli_surface_contract.py, framework/core/tests/python/test_xinfa_command.py, framework/core/tests/python/test_action_primitive_role_commands.py, framework/maintainability/abstraction-integrity-report.json, docs/qualification/evidence/agent-brief-first-value-loop/report.json, scripts/check-cli-catalog-parity.mjs, scripts/check-cli-catalog-parity.test.mjs, scripts/qualify-agent-first-value-codex.mjs, scripts/readonly-agent-bootstrap.test.mjs, crates/trunk/src/launch.rs, crates/xinfa/qualification/standalone-smoke-v1.json]
review_state: self-reviewed
sensitivity: public
sources: [local-files, user-consensus]
period: 2026-07-19
theme: kungfu-single-entry-action-primitive-cli
confidence: high
evidence_grade: B
last_reviewed: 2026-08-03
ai_provenance: GPT-5 via Codex on 2026-07-19; based on repository sources and user-authorized product constraints; no claim about unpublished release artifacts or unobserved platform behavior
---

# KF-ADR-019f86da-4f90-710c-a3b6-5e0cb5a28ad0: Kungfu is the only public Action Primitive CLI entrypoint

- Status: accepted; implementation staged
- Date: 2026-07-19
- Category: CLI topology / Action Primitive product / Xinfa distribution
- Supersedes: the public-CLI portions of [KF-ADR-019f86da-4f90-7ca2-8757-52f713bd3df8](KF-ADR-019f86da-4f90-7ca2-8757-52f713bd3df8.md) and [KF-ADR-019f86da-4f90-7a58-80ea-4666cc94397f](KF-ADR-019f86da-4f90-7a58-80ea-4666cc94397f.md)
- Preserves: Xinfa ownership and clean extraction from KF-ADR-019f86da-4f90-7ca2-8757-52f713bd3df8, Xinfa Atlas identity from KF-ADR-019f86da-4f90-7a58-80ea-4666cc94397f, and the Action host membrane from [KF-ADR-019f86da-4f90-7809-898a-ccc0f52bd390](KF-ADR-019f86da-4f90-7809-898a-ccc0f52bd390.md)

## Context

Kungfu v4 is distributed to people and Agents as one CLI product. Publishing a
second `xinfa` executable would make users choose between product entrypoints
before they understand the underlying roles, and would make one-shot Agent
onboarding incomplete. At the same time, folding Xinfa semantics into Click or
Core would destroy its independent schema, state, version, engine, and clean
extraction boundary.

## Decision

### 1. The public command tree has one executable

The terminal user installs and remembers only `kungfu`:

```text
kungfu
├── agent brief
├── xinfa compile
├── atlas capabilities|inspect|action
├── pursuit capabilities|inspect|action
├── warrant capabilities|inspect|action
└── episode capabilities|inspect|action
```

`kungfu xinfa compile` compiles the current workspace's
`.xinfa/project.json` into `.xinfa/atlas` and emits the JSON receipt unless an
explicit project, pack, root, workspace, output, or output mode is supplied.
There is no installed PATH launcher named `xinfa`.

### 2. Xinfa remains an independent engine, not a second user product entry

Xinfa continues to own `xinfa.*` schemas, `.xinfa` state and cache, its Cargo
version, compiler implementation, diagnostics, roots, receipts, and extraction
manifest. The installed archive carries its physical binary at a private,
manifest-bound engine path. The `kungfu xinfa` adapter only normalizes the
workspace Atlas shorthand and propagates engine stdout, stderr, and exit code.

The extracted physical binary and `./shifu xinfa` remain qualification and
source-development entries. They are not advertised as terminal-user product
entrypoints. Standalone qualification proves separability and semantic
ownership; it does not require a second public command.

### 3. Four role groups project one existing Profile engine

Atlas, Pursuit, Warrant, and Episode each expose capabilities, inspection, and
versioned action planning/execution under `kungfu`. These Click groups do not
copy transition tables or mutate storage directly. They select one role and
delegate to the existing KFD-7 `work_profile`, whose mutations cross the native
Fact kernel and named-ref CAS.

The `kungfu atlas` primitive exposes only its native role operations:
capabilities, inspect, and action. Action MJS may later own pure validation,
plans, and projections, but
Core remains the authority for identity, journal, CAS, Cuts, receipts, and
mutation.

### 4. `kungfu agent brief` starts one bounded first-value loop

The installed brief names the only public executable, the compiler command,
the four role groups, authority boundaries, safe discovery path, and explicit
non-claims. It is offline and does not initialize runtime state. Reading it is
not itself completion: the Agent verifies the installed documentation, selects
one declared intent, runs one read-only or preview-safe discovery, and emits
one product-owned first-value receipt that the user can verify independently.
Further capability commands deepen discovery but are not required to learn the
command topology.

The first-value contract is provider-free and deterministic. CI validates the
contract, schema, fail-closed cases, packaging, and retained receipt roots
without invoking a model. Experiential qualification is separate and bounded:
ten independent fresh macOS Codex sessions cover the canonical prompt and
declared natural-language variants while qualifying only their exact isolated
candidate and environment. They do not qualify Claude, CI-hosted Codex, other
platforms, or a public release.

The same one-entry rule governs root help: the default projection keeps the
four role objects and safe first actions visible, while explicit full, section,
and JSON discovery enumerate the complete contract without introducing a
second taxonomy or executable. Help remains offline and does not activate a
Profile or initialize workspace state.

PR #2209 carries that same Brief into every built-in no-task native Agent
launch. The Agent Session records an attempt-bound bootstrap receipt before a
meaningful task decision and exposes pending, verified, or degraded state
without claiming that the provider understood the text. This is an execution
of the existing one-entry decision, not a second onboarding authority.

## Falsification and acceptance

The decision is false if an installed archive exposes a second top-level
executable, `kungfu xinfa compile` differs from source Xinfa Atlas compilation,
the adapter rewrites engine JSON or errors, a role group accepts another role's
request, Click duplicates Profile transitions, onboarding creates runtime
state, or clean Xinfa extraction stops passing.

Qualification therefore checks the public topology contract, adapter argument
mapping and exit propagation, cross-role rejection, source/product compiler
parity, private engine staging, archive launcher inventory, the deterministic
first-value contract and receipt, and the existing standalone smoke.

The complete folded Click surface is also published as one deterministic,
root-bound Agent catalog. Source acceptance rejects catalog drift, orphaned
KFD-3 or Agent command links, undeclared public command references in docs,
skills, GUI and Agent Console, and omission from the packaged Agent inventory.

## Version impact

This adds `xinfa.product-contract/v2` and
`kungfu.action-primitive-cli/v1`. Existing `xinfa.*` object schemas, roots,
state paths, and Xinfa `0.1.0` engine version retain their meaning. The CLI
topology is additive to Kungfu's pre-release v4 line, except that a future
distributed standalone `xinfa` user launcher is now explicitly forbidden.

## Non-claims

This decision does not make ActionBinding a fifth primitive, complete the
generic Warrant product boundary, certify Pursuit completion from an action
receipt, remove Rust Xinfa, or claim every platform archive has passed before
its named product qualification runs.
