---
metadata_schema: kungfu.document-metadata/v1
doc_type: architecture-decision
adr_id: KF-ADR-019fe996-1912-7144-8fa5-3fceaa416365
decision_status: accepted
implementation_status: implemented
review_state: self-reviewed
sensitivity: public
implementation_prs: [https://github.com/kungfu-systems/kungfu/pull/2869, https://github.com/kungfu-systems/kungfu/pull/2908, https://github.com/kungfu-systems/kungfu/pull/2979, https://github.com/kungfu-systems/kungfu/pull/3291]
closure_pr: https://github.com/kungfu-systems/kungfu/pull/2869
qualification_refs: [scripts/check-temporal-relation-contract.test.mjs, framework/core/tests/python/test_temporal_relation.py, tests/fixtures/temporal-relation-contract/cases.json, framework/core/src/python/kungfu/storage/fact_root_canonical.py, scripts/check-release-provenance-object.test.mjs, framework/core/src/python/kungfu/release_provenance/__init__.py, framework/core/tests/python/test_release_provenance.py, tests/fixtures/release-provenance-object/cases.json, framework/release/kungfu-release-provenance.contract.json, framework/core/tests/python/test_temporal_release_admission.py, framework/core/tests/python/test_temporal_provenance_cutover.py, scripts/verify-kungfu-release-admission.test.mjs, docs/qualification/evidence/kungfu-temporal-release-admission-facts.json, docs/qualification/evidence/kungfu-temporal-provenance-cutover.json, framework/release/kungfu-durable-provenance-authority.contract.json, scripts/check-durable-provenance-authority.test.mjs, scripts/qualify-durable-provenance-authority.py, docs/qualification/evidence/durable-provenance-authority/v1/report.json, docs/qualification/evidence/durable-provenance-authority/v1/authority-bundle.json]
sources: [local-files, user-consensus]
period: 2026-08-10
theme: temporal-relation-fact-contract
confidence: high
evidence_grade: B
last_reviewed: 2026-08-18
ai_provenance: GPT-5 via Codex on 2026-08-10, 2026-08-12, 2026-08-15, and 2026-08-18; based on the accepted Fact kernel, immutable Assignments, and repository evidence; release vocabulary remains a bounded Profile projection and no production-publication claim is made
---

# KF-ADR-019fe996-1912-7144-8fa5-3fceaa416365: Immutable temporal relations and bounded proof paths are first-class Facts

- Status: accepted; implemented by commit `b35c3663e390def44dfa3440c669ae6eaa752aeb` and qualified by PR #2869
- Release projection: immutable candidate and promotion provenance objects are delivered by PR #2908 without adding release vocabulary to Core
- Temporal admission cutover: PR #2979 at functional commit `685ad133c7fd390dbf4accee89fdf8113482c2dd` makes historical and current allowances active only through closed fact proofs; the retained digest list is a generated rollback projection, not authority
- Durable authority cutover: PR #3291 makes the release-provenance Fact/Cut durability profile default-enabled and production-eligible, retains exact Alpha history and clean-host replay evidence, and leaves Git/GitHub coordinates as non-authoritative projections without performing publication
- Protected-delivery repair: a dequeued source identity is never reused; the replacement revision must recompute its Project Cut replay proof against the exact protected base while preserving the qualified Fact and qualification roots
- Date: 2026-08-10
- Category: Fact kernel / temporal relations / proof-carrying query
- Related: [KF-ADR-019f86da-4f90-7c45-8d95-3745dcbbff1c](KF-ADR-019f86da-4f90-7c45-8d95-3745dcbbff1c.md),
  [KF-ADR-019f86da-4f90-71eb-b4c0-376ca7bc7ad3](KF-ADR-019f86da-4f90-71eb-b4c0-376ca7bc7ad3.md), and
  [KF-ADR-019f86da-4f90-7f46-b195-3af6228d17b1](KF-ADR-019f86da-4f90-7f46-b195-3af6228d17b1.md)
- Machine contract: [`kungfu-fact-cut-kernel.contract.json`](../../framework/fact/kungfu-fact-cut-kernel.contract.json)

## Context

The Fact kernel already separates stable object identity, immutable versions,
typed relation roots, reproducible Cuts, append-only revocation, and
proof-carrying query. Its generic relation record deliberately does not define
whether one relation is compatibility, causality, provenance, supersession, or
delegation. Downstream systems have therefore repeated those meanings in
transport manifests, workflow state, mutable indexes, and current-head checks.

Those repetitions lose important temporal distinctions. A later revocation can
appear to invalidate a result that was correct at an earlier Cut. A directed
compatibility statement can be treated as symmetric. Two individually valid
edges can become an undeclared transitive assertion. A graph search can choose
one of several authority paths without exposing the ambiguity. Finally, a
receipt can name the current result without binding the exact path and Cut that
produced it.

The neutral Core needs one content-addressed language for these facts before a
release Profile, Buildchain projection, or provider adapter specializes them.
It must extend the existing KFR2 and Fact Cut authorities rather than create a
mutable graph database or a third schema substrate.

## Decision

### Closed temporal records use KFR2

Predicate declarations, relation assertions, supersessions, revocations,
authority proofs, provenance objects, explicit path queries, and their receipts
are closed records in `kungfu.fact-root.canonical/v2`. The machine registry owns
their field ids, and the independent C++ and Python encoders must agree on exact
bytes, roots, and rejection. JSON remains an edge representation.

Every record binds its interpretation inputs. Predicate declarations bind
direction, operations, path policy, cycle policy, and governing authority.
Relation assertions bind the predicate, ordered endpoints, first valid Cut,
scope, authority, and admission roots. A provenance object binds its subject,
materials, relations, Cut, authority, and admissions without assigning domain
meaning to those roots.

### Time is an immutable Cut, not a clock or head

Queries name one exact Fact Cut root. `validFromCutRoot`,
`effectiveCutRoot`, and `revokedAtCutRoot` are evaluated only through the
bounded Cut ancestry supplied with the query. Supersession and revocation append
new facts. They do not edit relation bytes, Cut bytes, or an earlier query's
result. A mutable ref may be resolved at an outer boundary, but it cannot enter
the temporal query as identity.

### Direction, scope, and composition are explicit

The verifier follows `sourceRoot` to `targetRoot`; it never reverses an edge.
The requested operation must appear both in the predicate declaration and in
the applicable authority proof. The verifier does not search a graph. A caller
supplies the complete ordered relation path and a maximum depth no greater than
32. Multiple edges are rejected unless the predicate explicitly permits
bounded composition; even then, the receipt proves only the supplied path and
does not create an inferred direct relation.

### Authority ambiguity fails closed

A relation asserted directly by the required governing authority needs no
delegation proof. Every other relation requires exactly one immutable,
operation-scoped authority proof valid at the requested Cut. No proof is
`authority-missing`; more than one is `ambiguous-authority`. The verifier does
not rank or silently choose competing proofs.

### Positive and negative results are receipts

Every semantically evaluated query produces an immutable KFR2 receipt. It binds
the query root, status, stable failure code, Cut root, ordered relation roots,
authority proof roots, and explicit omission roots. Orphan roots, unknown
predicates, ambiguous authority, cycles, unscoped operations, and implicit
transitive acceptance all reject deterministically.

The Python query surface is an independent semantic projection over immutable
Fact roots. It cannot write journal authority, mutate a Cut, discover an
alternate path, or promote a projection to authority.

## Consequences

- Release and Buildchain Profiles can project their own predicates and objects
  without adding release vocabulary to Core.
- Old-Cut replay remains stable after later supersession and revocation facts.
- Callers must retain exact Cut lineage and submit the path they want verified;
  convenient ambient-head and graph-search behavior is intentionally absent.
- KFR2 gains closed schema entries, increasing the compatibility obligation for
  both independent encoders.
- A rooted path receipt proves the bounded semantic decision only. It does not
  prove universal truth, external side effects, provider durability, or work
  completion.

## Falsification and qualification

The deterministic fixture and Python gate cover accepted old-Cut and successor
paths plus orphan roots, unknown predicates, ambiguous authority, forbidden
cycles, unscoped operations, direction reversal, implicit transitivity, and
revocation. The existing KFR2 corpus independently fixes one temporal predicate
record's exact bytes and root and checks the complete C++/Python schema registry
parity.

The bounded release projection adds independent Node and Python qualification
for candidate and promotion objects, explicit dev-cut and prior-alpha roots,
ordered relation roles, and a non-authoritative Git projection. Negative cases
cover swapped Cuts, missing acknowledgement, ambiguous authority, mismatched
trees, and re-rooted forged projection evidence while the legacy workflow
evidence remains byte-identical.

The v2 candidate successor removes Git topology from the semantic preimage.
An algorithm-tagged source-content root and explicit derivation,
acknowledgement, qualification, approval, authority, and contract relations
produce the same object root across root, linear, merge, reordered, octopus,
flattened, and ff-only projections. Exact Git coordinates remain separately
rooted evidence. The v1 reader remains unchanged, while migration retains the
complete predecessor bytes and emits an explicit `succeeds` relation and rooted
receipt. The release workflow cutover now computes a canonical file-set digest
in Candidate Patrol, passes the exact v2 envelope through immutable retained
evidence, and consumes its explicit history, qualification, approval-policy,
and authority roots during promotion. Parent sequence and ancestry remain
advisory projections; the cutover does not claim or execute publication.

The temporal admission cutover independently roots every allowance's reason,
scope, evidence, authority, and Cut. Its static and retained-material
qualification covers source, tree, artifact, runtime, contract, proof,
authority, ancestry, revocation, implicit-transitive, and orphan failures. The
historical Alpha archives and reconstructed candidate are hashed and compared
without rebuilding or publication; rollback preserves the immutable fact set,
provenance objects, and receipts.

Production release qualification is not claimed. It remains governed by the
existing Fact durability and release-admission decisions.
