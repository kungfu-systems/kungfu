---
metadata_schema: kungfu.document-metadata/v1
doc_type: architecture-decision
adr_id: KF-ADR-019fb2de-4a59-7bf4-8231-b32b62aa7e9b
decision_status: accepted
implementation_status: implemented
review_state: self-reviewed
sensitivity: public
sources: [user-consensus, local-files]
period: 2026-07-30
theme: project-work-agent-first-layer
confidence: high
evidence_grade: B
implementation_prs: [https://github.com/kungfu-systems/kungfu/pull/1991, https://github.com/kungfu-systems/kungfu/pull/2147, https://github.com/kungfu-systems/kungfu/pull/2185, https://github.com/kungfu-systems/kungfu/pull/2208, https://github.com/kungfu-systems/kungfu/pull/2225, https://github.com/kungfu-systems/kungfu/pull/2228, https://github.com/kungfu-systems/kungfu/pull/2379, https://github.com/kungfu-systems/kungfu/pull/2454, https://github.com/kungfu-systems/kungfu/pull/2626, https://github.com/kungfu-systems/kungfu/pull/2709, https://github.com/kungfu-systems/kungfu/pull/2769, https://github.com/kungfu-systems/kungfu/pull/3026, https://github.com/kungfu-systems/kungfu/pull/3029, https://github.com/kungfu-systems/kungfu/pull/3407]
closure_pr: https://github.com/kungfu-systems/kungfu/pull/1991
qualification_refs: [framework/product/project-work-agent.contract.json, scripts/check-project-work-agent-product.test.mjs, framework/core/tests/python/test_projects.py, framework/core/tests/python/test_run_product_path.py, framework/core/tests/python/test_cli_progressive_help.py, framework/core/tests/python/test_agent_work_lab.py, framework/core/tests/python/test_assignment_review_equivalence.py, framework/core/tests/python/test_agent_session_module_boundaries.py, framework/agent-session/tests/mock-provider.test.mjs, framework/agent-session/tests/product-surface.test.mjs, crates/trunk/src/help.rs, framework/api/tests/onboarding.test.ts, framework/tui/src/projects-view.test.ts, framework/tui/src/starter-project-view.test.ts, framework/tui/src/project-tour-view.test.ts, framework/tui/src/agent-first-onboarding-view.test.ts, framework/tui/src/agent-work-lab-view.test.ts, framework/gui/src/agent-work-lab.test.ts, extensions/work-dashboard/tests/work-control-profile.test.ts, extensions/work-dashboard/tests/project-work-run.test.ts]
ai_provenance: GPT-5 via Codex on 2026-08-09 recorded the PR 2626 corrective delivery that streams exact Project Tour authority progress, preserves fail-closed Work and review gates, aligns the bundled Mock Agent with its interaction adapter, and qualifies both isolated episodes from assembled CLI products; updated on 2026-08-10 to record PR 2709's single-process episode controller and bounded final Work reconciliation, then to record PR 2769's current-base replay after PR 2756 became conflicting and PR 2763 was recreated under the source account; updated on 2026-08-12 to record PR 3026's governed open, watch, tour, try, test, and report journey plus bounded startup observation and human-readable Agent selection, and PR 3029 as its Project Cut replay-qualified successor; updated on 2026-08-24 to record PR 3407's behavior-preserving GUI and TUI orchestration decomposition; no claim is made about protected merge, rendered media, site publication, or production readback
last_reviewed: 2026-08-24
---

# KF-ADR-019fb2de-4a59-7bf4-8231-b32b62aa7e9b: Project, Work, and Agent are the complete first-layer product model

- Status: accepted
- Date: 2026-07-30

## Context

Kungfu's authority model needs precise objects such as Initiative, Assignment,
Episode, Claim, Assessment, Decision, and Project Cut. Exposing all of them as
first-use navigation made a new user learn the implementation before they
could ask an Agent to do useful work. The product also projected overlapping
Workspace, Starter Project, Work Control, and Portfolio views, so the same
local repository appeared to be several different things.

The Alpha release needs one model that works in the TUI, GUI, and CLI without
weakening exact authority or retained evidence. A local Git repository is
already the object users understand. The product can bind it to `.kungfu`
without making that storage and authority layout a prerequisite concept.

## Decision

The complete first-layer object model is **Project, Work, Agent**.

1. A Project is one local directory and its retained Kungfu evidence.
   **Projects** is the persistent machine-local library of opened or
   initialized Project directories.
2. Work is one bounded outcome inside a Project. **All Work** is the active,
   completed, or complete machine-local projection across available Projects.
3. An Agent is the selected provider process that performs or independently
   reviews Work.

The first entry is not a replacement work interface. It teaches the user's
existing Agent the exact local Kungfu capability through `kungfu agent brief`.
GUI and TUI remain optional control and observation surfaces, with Agent Work
Lab and Project Tour available as progressive-disclosure learning routes.
README, CLI, GUI, TUI, Lab, and Tour must converge at that same point: first
make the bundled or standalone CLI discoverable, then give the existing Agent
the exact brief prompt, and complete shared onboarding only after durable Work
starts or binds. Completed or dismissed onboarding opens Core Work directly;
it does not require Lab inspection, KFX admission, or native renderer startup.
KFX execution and Work settlement retain their independent fail-closed gates.
After first use, low-frequency onboarding re-entry belongs in the GUI Help menu
and the TUI `/onboarding` command rather than permanent daily-work chrome.
Project Tour presents this model as two bounded episodes: the first establishes
Project, Work, and Agent through an ordinary delivery; the second resumes the
same retained Work to demonstrate failure recovery, independent review, and
settlement. Each episode is independently playable, while an `all` mode keeps
one continuous evidence story for CI and full demonstrations.
PR 2454 projects those episodes as separate 1x auditable-demo entries so each
episode keeps an independent completion sentinel, timing budget, and native
1080p/720p evidence chain without changing the continuous `all` route.

Native Project Tour work is not replaced by a synthetic timer. Run and review
events enter one source-ordered, deduplicating presentation queue as soon as
the authority operation emits them. While an admission, lease, kickoff,
review, claim, or settlement stage is genuinely active, its current line gains
an elapsed-seconds signal. The animation therefore distinguishes visible real
work from a frozen renderer without weakening any fail-closed boundary.

Each Project Tour episode is orchestrated by one CLI controller process. That
controller calls the existing native Project, Work, Attempt, review, and
settlement authorities directly, streams their admitted events, and performs
one final authoritative Work inventory reconciliation at the episode boundary.
It does not introduce another Work state machine or replace fail-closed native
admission. The TUI consumes this event stream instead of repeatedly cold-starting
the CLI and re-querying the same Work inventory between narrative scenes.

Agent Work Lab exposes the same learning route through the bounded beginner
journey `open`, `watch`, `tour`, `try`, `test`, and `report`. Watch and Tour are
playback-only, Try creates a persistent Starter Project, Test keeps real Agent
execution behind explicit admission, and Report reopens retained evidence
without inventing success. Human-readable Agent selection delegates to the
same exact profile identities, while startup inspection consumes a bounded
verified observation instead of synchronously scanning the global Work graph.

The bundled Mock Agent and its interaction adapter are one tested product
pair. A terminal Mock scenario reaches an Agent-finished boundary only after
the process has actually exited; review-ready text alone is not an input
prompt, and an ended Capsule is no longer projected as a live controllable
Agent. This keeps process completion, independent review, and Work settlement
as three separate facts in Episode 2.

Every interactive surface presents the same golden path:

```text
Teach the existing Agent with kungfu agent brief -> New or Open Project -> Create or select Work -> Run Agent -> Review and complete Work
```

The CLI projects the same model through `kungfu project ...` and
`kungfu run <agent> [task]`. The no-argument `kungfu` entry remains the TUI;
there is no public `kungfu tui` compatibility alias.
Default `kungfu --help` exposes only `agent`, `project`, and `run` command
families under the Project, Work, Agent model. Existing expert commands remain
directly callable and are discoverable through `--help-all` and
`--help-section`, without occupying the first-use navigation layer.

Initiative, Assignment, Episode, Claim, Assessment, Decision, Project Cut,
Profile, and workspace/data-home terminology remain valid internal authority
or detail-layer concepts. They may appear in evidence, diagnostics, APIs, and
explicit explain views. They must not become first-layer navigation, calls to
action, or prerequisites.

The executable boundary is
`framework/product/project-work-agent.contract.json`, qualified by
`./shifu check:project-work-agent-product`. Internal authority contracts remain
the source of truth for admission, execution leases, review, and completion;
the product shell does not acquire that authority.

PR 3407 decomposes the GUI shell and the TUI starter-project and Work-window
input orchestration into cohesive hooks and pure state machines. The change
preserves the first-layer Project, Work, and Agent vocabulary, navigation, and
native authority effects while reducing the complexity concentrated in those
surface controllers.

## Consequences

Users can start from an existing repository without learning Kungfu-specific
ontology. TUI, GUI, and CLI can evolve independently while their first-layer
objects and golden path remain mechanically checked. Projects become a
machine-wide locator, while `.kungfu` remains Project-local evidence.

The cost is a deliberate progressive-disclosure layer: diagnostics and explain
views must translate between Work and the exact internal Assignment and Episode
coordinates. New first-layer objects require a superseding ADR and contract
change, rather than appearing opportunistically in one surface.

This decision is falsified if a fresh user must choose an Initiative,
Assignment, Profile, Workspace, or Portfolio to start ordinary Work; if the
same Project cannot be found from Projects; if All Work omits retained
Project-local Work; or if a public surface launches an Agent without the exact
Work authority plan.

## Corrective qualification

On 2026-08-09, the assembled macOS arm64 CLI product qualified Episode 1 in
84.43 seconds at 4x with report root
`sha256:cd6954bfde9aa47873542769aa4dc90a416eb12fb53b67d259cebb97867162ba`.
Its native Work admission remained visible for about 17 seconds and execution
lease acquisition for about 11 seconds instead of presenting an inert frame.

After correcting Mock Agent version drift and the ended-Capsule live
projection, a fresh assembled CLI product qualified Episode 2 in 104.76
seconds at 4x with report root
`sha256:eac58d903c561af8c5b4bcef8a8739f25d2d8a19d9929749275c5eb338f5098f`.
The recovery attempt, independent review, stage-ready claim, and settlement
all used native authority paths and emitted live stage progress. These are
local product qualifications, not claims about published media or production
endpoints.
