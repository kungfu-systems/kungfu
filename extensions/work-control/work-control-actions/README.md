# Work Control Actions

Owns the active Initiative/Assignment/Claim domain, assessment orchestration,
and portable evidence bundles. `adapter.py` is the exact-root public Profile
boundary; `domain/` is loaded only from this member's content-bound package
root.
